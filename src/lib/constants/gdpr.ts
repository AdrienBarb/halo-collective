// Bumped whenever the consent text materially changes (legal copy, scope of
// processing, lawful basis). Stored on the HubSpot contact so we can replay
// which version a fan agreed to — required if a regulator ever asks for proof.
export const HALO_GDPR_VERSION = "v1";
